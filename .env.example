# Add these as GitHub Actions repository secrets. Never paste actual keys into files.
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
# Optional. Add only after confirming your analyst-data access and display terms.
FINNHUB_API_KEY=
# Paper bot controls. The code accepts only Alpaca's paper-trading base URL.
ENABLE_AUTO_PAPER_TRADING=false
PAPER_BUDGET_USD=10000
